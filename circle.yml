general:
  build_dir: src

dependencies:
    pre:
        - mkdir -p $GOPATH/bin
        - export PATH="$GOPATH/bin:$PATH"
        - curl https://glide.sh/get | sh
    override:
        - glide install
        - go build -v
