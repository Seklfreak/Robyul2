dependencies:
    override:
        - sudo add-apt-repository ppa:masterminds/glide -y
        - sudo apt update
        - sudo apt install glide -y

test:
    pre:
        - glide install
        - go build -v
